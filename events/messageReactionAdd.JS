exports.run = (event, discord, fs, config, keys, bot, emojis) => {
    
    //EXPERIMENTAL
    
    try {
        
        //msg.channel.fetchMessage();

        //let reactedEmoji = event.emoji.identifier;
        //console.log(reactedEmoji);
        return;
    } catch (e) {
        return;
    }
}
